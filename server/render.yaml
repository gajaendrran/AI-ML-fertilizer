services:
  - type: web
    name: your-service-name
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python --version && python your_model_script.py"
    pythonVersion: 3.11.9 